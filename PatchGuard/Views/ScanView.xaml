<UserControl x:Class="PatchGuard.Views.ScanView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:controls="clr-namespace:PatchGuard.Controls"
             xmlns:conv="clr-namespace:PatchGuard.Converters"
             mc:Ignorable="d"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             d:DesignHeight="600" d:DesignWidth="1000">
    <UserControl.Resources>
        <conv:ProgressStatusConverter x:Key="ProgressStatusConverter"/>
    </UserControl.Resources>

    <Grid Style="{StaticResource PageRoot}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <controls:PageHeader Grid.Row="0"
                             Title="{Binding ScenarioTitle}"
                             Eyebrow="DIAGNOSTIC SCAN"
                             Subtitle="Running diagnostics — read-only modules only."/>

        <controls:JourneyStepIndicator Grid.Row="1" CurrentStep="2"/>

        <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto" Padding="0,0,8,0">
            <ItemsControl ItemsSource="{Binding ProgressItems}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Style="{StaticResource CardBorder}">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="140"/>
                                </Grid.ColumnDefinitions>
                                <StackPanel Margin="0,0,24,0">
                                    <TextBlock Text="{Binding ModuleName}" FontWeight="SemiBold" FontSize="15"
                                               Foreground="{StaticResource TextPrimaryBrush}" Margin="0,0,0,10"/>
                                    <TextBlock Text="{Binding Message}" TextWrapping="Wrap" Style="{StaticResource SubtleText}"/>
                                </StackPanel>
                                <TextBlock Grid.Column="1"
                                           HorizontalAlignment="Right"
                                           VerticalAlignment="Center"
                                           Foreground="{StaticResource AccentBrush}"
                                           FontWeight="SemiBold"
                                           Text="{Binding Status, Converter={StaticResource ProgressStatusConverter}}"/>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,32,0,0">
            <TextBlock Text="{Binding ErrorMessage}" Foreground="#FF453A" VerticalAlignment="Center"
                       Margin="0,0,24,0" TextWrapping="Wrap" MaxWidth="400"/>
            <Button Content="Cancel scan" Style="{StaticResource SecondaryButton}" Command="{Binding CancelScanCommand}" Margin="0,0,16,0"
                    AutomationProperties.Name="Cancel scan and return"/>
            <Button Content="Back" Style="{StaticResource SecondaryButton}" Command="{Binding GoBackCommand}"
                    AutomationProperties.Name="Cancel scan and go back"/>
        </StackPanel>
    </Grid>
</UserControl>
